<Window x:Class="NIkita.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NIkita"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.15*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="0.2*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="0.15*"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="MyText" />
                <Border Grid.Column="1" Margin="10" Background="#FF509ADA" CornerRadius="10" >
                    <Button Margin="3" Background="#FF509ADA" Content="Send" BorderBrush="#FF509ADA"/>
                </Border>
            </Grid>
            <Grid Grid.Row="1">
            <ListBox x:Name="YourMess" Grid.Column="1"
                    ItemsSource="{Binding Messages}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                                <TextBlock FontSize="16" Text="{Binding Path=User}" TextAlignment="Center">
                                <TextBlock  FontSize="16" Text="{Binding Path=Text}" />
                            </TextBlock>
                            <TextBlock FontSize="10" Text="{Binding Path=Date}"/>
                            </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            </Grid>
        </Grid>
        <ListBox Name="AllUsers"></ListBox>
    </Grid>
</Window>
